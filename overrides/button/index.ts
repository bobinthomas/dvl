/**
 * Hand-written behaviour for Button, imported by the generated component.
 * This is the one explicit escape hatch: real interaction logic a
 * generator cannot express (focus traps, ripple effects, debouncing)
 * lives here, never in generated/. Empty until Phase 2 wires it in.
 */
export {};
